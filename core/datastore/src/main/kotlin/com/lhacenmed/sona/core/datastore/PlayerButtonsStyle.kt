package com.lhacenmed.sona.core.datastore

/** What colours the player's play button and seek bar: ArchiveTune's `PlayerButtonsStyle`. */
enum class PlayerButtonsStyle {
    /** The player's own text colour. */
    DEFAULT,

    /** The theme's secondary colour. */
    SECONDARY,
}
