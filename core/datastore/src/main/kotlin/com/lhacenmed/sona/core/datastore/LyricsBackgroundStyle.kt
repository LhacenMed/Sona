package com.lhacenmed.sona.core.datastore

/**
 * What the lyrics sheet is drawn over: ArchiveTune's `LyricsBackgroundStyle`, without the custom image
 * Sona's player has no setting for.
 */
enum class LyricsBackgroundStyle {
    DEFAULT,
    FOLLOW_THEME,
    COLORING,
}
