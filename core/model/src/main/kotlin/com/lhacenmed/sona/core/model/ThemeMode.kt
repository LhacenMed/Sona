package com.lhacenmed.sona.core.model

/** Whether the app is drawn light or dark: as the system is, or always one of the two. ArchiveTune's `DarkMode`. */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
}
