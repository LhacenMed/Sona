<?xml version="1.0" encoding="utf-8"?>
<!-- About reads the licenses the AboutLibraries plugin writes by name, which the release build's
     resource shrinker cannot see - so it is kept here. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@raw/aboutlibraries" />
